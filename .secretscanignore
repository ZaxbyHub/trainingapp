# Known false-positive patterns for secretscan and placeholder_scan
#
# Each entry must be a specific verified false-positive pattern.
# Do NOT add broad directory or extension exclusions here.
# Do NOT add test directories — they contain legitimate test assertions,
# not secrets, but broad exclusion hides real findings.
#
# Format: glob patterns matching files or directories to exclude

# Generated/build output files (not source, no actionable findings)
**/__pycache__/**
*.pyc
*.pyo
**/dist/**
**/build/**

# External/vendored code (not our codebase)
**/node_modules/**
