<!-- PLAN_HASH: mp9kvhp3bc4g -->
# Offline-First Hardening & Model Upgrade
Swarm: paid
Phase: 1 [COMPLETE] | Updated: 2026-04-15T01:52:58.477Z

---
## Phase 1: Backend Refactor — Remove non-GGUF backends, add Gemma 4 support [COMPLETE]
- [x] 1.1: Add DEFAULT_BUNDLED_GGUF constant and get_bundled_model_path() function to app_paths.py. The constant holds 'gemma-4-E2B-it-Q5_K_M.gguf'. The function searches for the default model first, then falls back to legacy paths (phi3-mini-int4.gguf, phi3.5-mini-instruct-int4-cw-ov, test_model.gguf). Covers FR-103. [SMALL]
- [x] 1.2: Remove OpenVINOLLM class (lines 83-144), OllamaLLM class (lines 305-403), and OpenAICompatibleLLM class (lines 406-517) from llm_interface.py. Then simplify SmartLLM (lines 551-733) to only support GGUFBackend — remove multi-backend fallback chain, Ollama/API parameters, and backend iteration loop. Covers FR-104, FR-105, FR-106, FR-107. [MEDIUM] (depends: 1.1)
- [x] 1.3: Add Gemma 4 model detection to GGUFBackend in llm_interface.py. Check for 'gemma-4' or 'gemma_4' in model filename. When detected, add '<|think|>' token to the stop parameter in chat_complete() and generate() to suppress thinking mode for RAG use. Covers FR-108. [SMALL]
- [x] 1.4: Remove online parameters from RAGEngine.__init__() and _init_llm() in rag_engine.py. Remove: ollama_model, ollama_url, api_url, api_model, device parameters. Covers FR-109. [SMALL]
- [x] 1.5: Remove online parameters from engine_factory.py. In create_engine(), create_engine_from_settings(), and create_engine_from_env(): remove ollama_model, ollama_url, api_url, api_model params. Remove model_path backward-compat param and RAG_MODEL_PATH env var handling. Replace inline bundled model lists with get_bundled_model_path() from app_paths.py. Covers FR-110. [MEDIUM] (depends: 1.1)
- [x] 1.6: Remove dead code in main.py that references undefined args (model_path, ollama_url, ollama_model, api_url) at lines 61-68. Also remove RAG_MODEL_PATH environment variable handling. Note: create_parser() does NOT define these CLI args — they are dead references in the usage code. Covers FR-111. [SMALL]
- [x] 1.7: Remove environment variable validations from api_server.py lifespan: RAG_MODEL_PATH, RAG_OLLAMA_MODEL, RAG_OLLAMA_URL, RAG_API_URL, RAG_API_MODEL, RAG_DEVICE. Remove model_path and device parameters from RAGEngine() construction at line 461. Covers FR-112. [SMALL]
- [x] 1.8: Update requirements.txt: remove openvino and openvino-genai lines (lines 13-14), update llama-cpp-python constraint to >=0.2.30. Covers FR-201, FR-203. [SMALL]

---
## Phase 2: Resilience & Code Quality — Embedding hardening, lazy init, error handling [COMPLETE]
- [x] 2.1: Harden EmbeddingModel in vector_store.py: always pass local_files_only=True to SentenceTransformer in both PyInstaller and dev mode; add ./models/bge-small-en-v1.5/ as secondary search path; raise clear FileNotFoundError with expected path when model not found. Covers FR-301, FR-302, FR-303. [MEDIUM]
- [x] 2.2: Implement lazy initialization in vector_store.py: defer BM25 index rebuild to first bm25_search() call. Make SentenceTransformer lazy — load on first encode()/encode_single() call, not in __init__(). On BM25 failure, set empty BM25Index instance with warning. Add BM25 search failure logging. Move STOP_WORDS import to module level. Covers FR-501, FR-502, FR-604, FR-606, FR-804. [MEDIUM] (depends: 2.1)
- [x] 2.3: Wrap LLM call in query_transformer.py in try/except. On failure, log warning and return original query unchanged. Replace all print() calls in llm_interface.py with logger.warning() or logger.info(). Covers FR-601, FR-602. [SMALL]
- [x] 2.4: Replace remaining print() calls in document_processor.py (lines 359, 366, 367, 383, 385) with logger.warning() or logger.info(). Verify adequate structured logging exists at document processing start/completion (file name, chunk count). If gaps exist, add them. Covers FR-603, FR-602. [SMALL]
- [x] 2.5: Add try/except wrappers in rag_engine.py: wrap reranker initialization with fallback to None + warning; wrap _save_config() with user-facing error message. Extract _log_init_banner() helper from __init__() to eliminate duplicated logger.info banner blocks. Gate query_transformer call on RAGConfig.query_transformation_enabled flag. Covers FR-605, FR-607, FR-802, FR-805. [MEDIUM] (depends: 1.4)
- [x] 2.6: Code quality fixes: extract _resolve_and_validate_path() helper in api_server.py to share duplicated path-resolution logic (FR-801); move 'from collections import defaultdict' import to module level in utils.py (FR-803); fix _SettingsProxy in config.py to wrap AttributeError with informative message (FR-806); simplify security.py URL validation to reject non-local URL schemes (FR-901). Covers FR-801, FR-803, FR-806, FR-901. [MEDIUM]
- [x] 2.7: Defer _load_settings() in app_gui.py with self.after(50, self._load_settings_and_init) to allow first render before blocking I/O. Note: wrap _load_settings() call at line 381 and the subsequent widget initialization code in the deferred function. There is no existing _load_settings_and_init method — create one that combines _load_settings() and _initialize_widgets(). Covers FR-503. [SMALL]

---
## Phase 3: UI/UX & Settings — Settings cleanup, _initialize_engine fix, UI polish [COMPLETE]
- [x] 3.1: Fix _initialize_engine() bypass in app_gui.py: replace direct RAGEngine construction (lines 592-649) with engine_factory.create_engine_from_settings(). Remove Ollama/API parameter passing. Covers FR-405. [MEDIUM] (depends: 1.4, 1.5)
- [x] 3.2: Remove all online-related widgets and methods from SettingsDialog in app_gui.py: ollama_url_entry, ollama_model_entry, api_url_entry widgets; _test_ollama() and _test_api() methods; backend priority label text at line 103. Stop reading/writing ollama_url, ollama_model, api_url in _populate_fields() and _save(). Remove bundled model duplicate list in _load_settings() (lines 402-406). Covers FR-401, FR-402, FR-403, FR-404, FR-110. [MEDIUM]
- [x] 3.3: Add keyboard shortcuts to app_gui.py: Ctrl+Enter (submit question), Ctrl+L (clear chat), Ctrl+, (open settings). Add confirmation dialog before clearing chat. Style Ask button as primary, Clear button as secondary. Style Save button in SettingsDialog as primary. Covers FR-701, FR-702, FR-703, FR-704. [MEDIUM]
- [x] 3.4: Add progress bar improvements in app_gui.py: display percentage and phase labels. Add animated Thinking state (rotating ellipsis). Handle WM_DELETE_WINDOW with confirmation during active operations. Covers FR-705, FR-706, FR-707. [MEDIUM]
- [x] 3.5: Accessibility and polish in app_gui.py: set minimum CTkButton height of 44px globally for WCAG 2.5.5 compliance; call focus_set() on first focusable widget in SettingsDialog; specify 'Segoe UI' as font family for Windows consistency. Covers FR-708, FR-709, FR-710. [SMALL]

---
## Phase 4: Build System, Tests & Documentation — Packaging, test updates, doc cleanup [COMPLETE]
- [x] 4.1: Update build system files: root build.py — update default model path to Gemma 4; scripts/build.py — add Gemma 4 model validation, remove OpenVINO build steps; scripts/build_installer.py — update Qwen3-1.7B references to Gemma 4 E2B (lines 134-156 README generation); AFOMIS.spec — add models/ directory to datas list. Update run_api.bat to remove Ollama environment variables. Covers FR-1001, FR-1002, FR-1003, FR-1004, FR-1005. [MEDIUM] (depends: 1.8)
- [x] 4.2: Update test files: remove all mocks/imports of OllamaLLM, OpenAICompatibleLLM, OpenVINOLLM from test_llm_interface.py, test_rag_pipeline_edge_cases.py, test_phase1_fixes.py, test_workflows.py, integration/conftest.py, regression/test_defect_002_api_gguf_env.py. Replace with GGUFBackend mocks. Add dedicated GGUFBackend tests: Gemma 4 detection, thinking mode suppression, error handling. Covers FR-1101, FR-1102, FR-1103. [LARGE] (depends: 1.2, 1.3, 1.5, 1.7)
- [x] 4.3: Update documentation files to remove all OpenVINO references and update model names: ARCHITECTURE.md (remove OpenVINO backend docs, describe GGUF-only), CONFIGURATION.md (remove OpenVINO config references at lines 49, 298, 322, 326, 479, 496), INSTALL.md (remove OpenVINO install instructions at lines 31-32), README.md (remove OpenVINO docs, update model references to Gemma 4). Add CHANGELOG.md entry for model upgrade and backend removal. Covers FR-1201, FR-1202, FR-1203, FR-1204, FR-1205. [MEDIUM]
